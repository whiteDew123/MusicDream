.rank-page {
  padding: 32rpx 24rpx 0;
}
.page-title {
  font-size: 40rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 28rpx;
  padding: 0 8rpx;
}
.list {
  background: var(--bg-card);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-card);
  overflow: hidden;
}
.list-skeleton { height: 600rpx; }
.bottom-space { height: 120rpx; }
